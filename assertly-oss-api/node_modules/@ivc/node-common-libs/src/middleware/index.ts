import helmet from 'helmet';

export const initMiddleware = (app: any) => {

  // security settings
  app.use(helmet({
    // allow internal domains to be in an iframe
    frameguard: false,
    // allow things like PDFs to be previewed without forcing downloads in IE11
    ieNoOpen: false
  }));
};

