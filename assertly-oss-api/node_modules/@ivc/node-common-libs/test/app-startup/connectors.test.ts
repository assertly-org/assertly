import { initConnections } from '../../src/';

describe('Initialize empty connections', () => {
  it('should return json', async (done) => {
    const config = {};
    const connRes = await initConnections({ config }); 
    console.log(connRes);
    expect(connRes).toMatchObject({});
    done();
  });
});
