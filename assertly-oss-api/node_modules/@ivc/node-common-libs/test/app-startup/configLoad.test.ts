import { loadConfig } from '../../src/';

describe('Load a basic config', () => {
  it('should return json', () => {
    const config = loadConfig();
    expect(config).toMatchObject({appName: 'node-common-libs'})
  });
});
